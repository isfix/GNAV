package com.example.pandu_navigation

import io.flutter.embedding.android.FlutterActivity

// MainActivity - simplified after GraphHopper removal
// The app now uses GPX-based "Strict Track Following" instead of native routing
class MainActivity : FlutterActivity() {
    // No native routing needed - all logic is in Dart/Flutter
    // Using GPX files + DeviationEngine for trail following
}
